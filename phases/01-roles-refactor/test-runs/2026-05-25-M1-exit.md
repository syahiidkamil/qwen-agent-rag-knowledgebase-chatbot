# M1 Exit Verification — 2026-05-25

**Phase:** 01-roles-refactor  
**Milestone:** M1 — BE role enforcement + seed script + FE RoleGuard  
**Executed by:** ATLAS (QA Manual via Playwright MCP)  
**Date:** 2026-05-25  
**Environment:** BE http://localhost:3081 · FE http://localhost:5181 · Live Supabase project `twwfxxeusssrwetdlixy`

---

## Summary

| TC | Title | Status | Note |
|---|---|---|---|
| TC-01-001 | All three seeded roles can sign in | partial-pass | super_admin + admin: full pass. user: sign-in succeeds, redirects correctly to `/workspace`, landing is the 404 placeholder — expected at M1 per TASK-01-006 scope. |
| TC-01-008 | User role cannot reach admin endpoints | pass | All four in-scope endpoints return HTTP 403 + structured FORBIDDEN body; FE `/admin` redirect to `/workspace` confirmed. |

**Overall M1 gate: PASS** (no regressions; partial on TC-01-001 user half is by design).

---

## TC-01-001 — All three seeded roles can sign in

### super_admin (`super.boss@airanext.id`)

| Step | Expected | Actual | Result |
|---|---|---|---|
| Sign in | No error, no 401 in console | Immediate redirect to `/admin/cms`, zero console errors | PASS |
| Landing page | Admin surface | `/admin/cms` with full sidebar (Landing CMS, Knowledge base) | PASS |
| User display | Identifies correct user | Sidebar shows "Super / super.boss@airanext.id" | PASS |
| Sign out | Redirects to `/login` | `button.admin-user-logout` → redirected to `/login` | PASS |

### admin (`admin@airanext.id`)

| Step | Expected | Actual | Result |
|---|---|---|---|
| Sign in | No error, no 401 in console | Immediate redirect to `/admin/cms`, zero console errors | PASS |
| Landing page | Admin surface | `/admin/cms` with full sidebar | PASS |
| User display | Identifies correct user | Sidebar shows "Admin" | PASS |
| Sign out | Redirects to `/login` | Redirected to `/login` | PASS |

### user (`cs.demo@airanext.id`)

| Step | Expected | Actual | Result |
|---|---|---|---|
| Sign in | No error, no 401 in console | Immediate redirect to `/workspace`, favicon 404 only in console (unrelated) | PASS |
| Landing page | `/workspace` (internal chat surface) | `/workspace` — renders 404 placeholder | PARTIAL-PASS (expected at M1; full page ships in TASK-01-006) |
| Not on admin surface | Should NOT land on `/admin/*` | Confirmed: URL is `/workspace`, not `/admin/*` | PASS |
| Sign out | N/A for M1 | Skipped (user workspace not built yet) | N/A |

**Evidence:**  
- `misc/screenshots/TC-01-001-super-admin-landing.png`  
- `misc/screenshots/TC-01-001-admin-landing.png`  
- `misc/screenshots/TC-01-001-user-landing.png`

---

## TC-01-008 — User role cannot reach admin endpoints

**JWT source:** Live Supabase sign-in as `cs.demo@airanext.id`. Token extracted from `localStorage['sb-twwfxxeusssrwetdlixy-auth-token'].access_token`. JWT `user_metadata.role` = `"user"`.

### API checks (curl with Bearer token)

| Endpoint | Expected | Actual HTTP | Body `code` | Result |
|---|---|---|---|---|
| `GET /api/documents` | 403 FORBIDDEN | 403 | `FORBIDDEN` | PASS |
| `POST /api/documents` | 403 FORBIDDEN | 403 | `FORBIDDEN` | PASS |
| `DELETE /api/documents/{uuid}` | 403 FORBIDDEN | 403 | `FORBIDDEN` | PASS |
| `PUT /api/landing-config` | 403 FORBIDDEN | 403 | `FORBIDDEN` | PASS |
| `GET /api/users` | SKIPPED | — | — | N/A (ships in TASK-01-007 / M3) |
| `POST /api/users` | SKIPPED | — | — | N/A (ships in TASK-01-007 / M3) |

Error body format confirmed on all four:  
```json
{"detail":{"error":{"code":"FORBIDDEN","message":"This action requires one of: admin, super_admin"}}}
```

### FE RoleGuard check

| Step | Expected | Actual | Result |
|---|---|---|---|
| Navigate to `/admin` as signed-in user | Redirect away from admin surface | URL became `/workspace` immediately; no admin content shown; zero FE console errors | PASS |

**Evidence:**  
- `misc/screenshots/TC-01-008-user-admin-redirect.png`

---

## Issues Found

None. No regressions. All M1 deliverables behave as specified.

---

## Scope Notes

- `GET /api/users` and `POST /api/users` skipped — not implemented at M1 (TASK-01-007, M3). Would return 404, not 403; irrelevant for this gate.
- User landing on `/workspace` 404 is correct M1 behavior. Full pass on the user-role half of TC-01-001 depends on TASK-01-006 shipping in M2.
- The single console error across all sessions was `favicon.ico 404` — pre-existing, unrelated to auth or role logic.
