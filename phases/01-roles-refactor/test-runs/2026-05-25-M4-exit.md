# M4 Exit Verification — 2026-05-25

**Phase:** 01-roles-refactor
**Milestone:** M4 — KB document rename + phase closeout
**Executed by:** ATLAS (Playwright MCP)
**Date:** 2026-05-25
**Environment:** BE http://localhost:3081 · FE http://localhost:5181 · Live Supabase project `twwfxxeusssrwetdlixy`

---

## Summary

| TC | Title | Status | Note |
|---|---|---|---|
| TC-01-003 | Admin renames a knowledge-base document | pass | Filename updated; storage object untouched; source chip on the next chat answer shows the renamed value. |
| TC-01-004 | Delete cascade | not re-run | Verified earlier in M1 sweep; no changes to delete path since. |
| TC-01-012 | KB upload regression | not re-run | Verified end-to-end in M2 (qna-aira-next.pdf still ingested with 6 chunks); rename does not touch the ingest pipeline. |

**Overall M4 gate: PASS** for the new work (rename + validation). Regression TCs (004, 012) were spot-checked against current state but not re-executed step-by-step in this session because nothing in M4 modifies the ingest or delete code paths.

Two bonus validations not in the original TC list were executed:

- **Rename input validation (server-side):** filename with `/` is rejected with `400 INVALID_FILENAME` and the message "filename cannot contain path separators or null bytes" surfaces inline in the dialog.
- **Pre-existing FE envelope bug (caught earlier in M3 sweep, fixed at commit `63c7bf4`):** the FastAPI `{detail: {error}}` envelope now unwraps correctly, so all structured backend errors render with the intended user-facing message instead of axios's generic `Request failed with status code N`. This was the underlying reason the M3 EMAIL_EXISTS toast looked generic; same path now used by INVALID_FILENAME above.

Two latent bugs surfaced and fixed during the M3+M4 sweep:

| Commit | What |
|---|---|
| BE `f14436f` | `_looks_like_email_conflict` heuristic missed gotrue's actual phrasing ("already been registered" with "been" between "already" and "registered"); duplicate-email submits returned 502 instead of 409. |
| FE `63c7bf4` | axios interceptor only read top-level `data.error`; FastAPI returns `{detail: {error: {...}}}`, so every structured backend error showed axios's generic message instead of the backend's intended copy. |

---

## TC-01-003 — Admin renames a knowledge-base document

### Precondition check
- Signed in as `super.boss@airanext.id` (super_admin transitively passes `require_role("admin")`).
- 1 document ingested: `qna-aira-next.pdf` with 6 chunks.

| Step | Expected | Actual | Result |
|---|---|---|---|
| Click Rename on the row | Dialog opens with filename pre-filled | Dialog opens, input contains `qna-aira-next.pdf`, focus + basename-selected | PASS |
| Replace with `Aira Next — FAQ (renamed via M4 test).pdf` and Save | Row refreshes with new filename | Row immediately shows the new filename via optimistic update; backend PATCH returns 200 | PASS |
| Chunks + status unchanged | `ingested`, 6 chunks | `ingested`, 6 chunks | PASS |
| Ask a question in the chat widget | Source chip shows renamed filename | Source chip: `[1] Aira Next — FAQ (renamed v…` (truncated); underlying URL still points at the original Storage path `qna-aira-next.pdf` | PASS |

**Evidence:**
- `repos/react-vite-frontend/M4-test-01-renamed-row.png`
- `repos/react-vite-frontend/M4-test-02-source-chip-renamed.png`

---

## TC-01-003-extra — Server-side validation surfaces inline (path separator)

| Step | Expected | Actual | Result |
|---|---|---|---|
| Open Rename dialog | Dialog opens | Opens with current filename pre-filled | PASS |
| Replace with `with/slash.pdf` and Save | Inline error, dialog stays open | Inline error "filename cannot contain path separators or null bytes" surfaces; dialog remains open with the bad value | PASS |
| Click Cancel | Dialog closes, row name unchanged | Closes; row still shows the previously-renamed filename | PASS |

Backend `_validate_filename` covers: empty, whitespace-only, > 255 chars, `/`, `\`, `\0`. Verified by `python -c` unit-style call from the venv before E2E.

**Evidence:**
- `repos/react-vite-frontend/M4-test-03-invalid-filename.png`

---

## Doc updates included in this milestone

- `docs/system-design/README.md` — `landing_config` snapshot now mentions the `chat_mode` field; new "Roles and authorization" section + surface-matrix table; brief note on `require_role` hierarchy and `get_current_user_optional` for anonymous chat.

---

## Phase 01 — overall closeout state

| Milestone | Gate | Date |
|---|---|---|
| M1 — Security hole + role infra | PASS (2026-05-25 M1-exit) | earlier this session |
| M2 — User chat + chat-mode gating + workspace | PASS (2026-05-25 M2-exit) | earlier this session |
| M3 — Admin user management | PASS (verified via M3 sweep in this session; 2 bugs found + fixed) | this session |
| M4 — KB rename + closeout docs | PASS (this doc) | this session |

The phase is closeout-ready: the client can deploy.
